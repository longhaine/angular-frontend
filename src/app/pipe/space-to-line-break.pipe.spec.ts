import { SpaceToLineBreakPipe } from './space-to-line-break.pipe';

describe('SpaceToLineBreakPipe', () => {
  it('create an instance', () => {
    const pipe = new SpaceToLineBreakPipe();
    expect(pipe).toBeTruthy();
  });
});
