import { LineBreakToSpacePipe } from './line-break-to-space.pipe';

describe('LineBreakToSpacePipe', () => {
  it('create an instance', () => {
    const pipe = new LineBreakToSpacePipe();
    expect(pipe).toBeTruthy();
  });
});
